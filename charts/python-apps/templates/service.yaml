apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-apps.fullname" . }}
  labels:
    {{- include "python-apps.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python-apps.selectorLabels" . | nindent 4 }}
